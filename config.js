var config = {}

/**
* Port that the server will run on
*/
config.PORT = 3000;
/**
* Total number of bulbs that should be generated on the client
*/
config.TOTAL_BULBS = 10;
/**
* The interval in which the colors of the bulbs should be updated
*/
config.LIGHT_UPDATE_INTERVAL = 500;

module.exports = config;
